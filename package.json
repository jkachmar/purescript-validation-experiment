{
  "scripts": {
    "postinstall": "bower install && pulp build",
    "validate": "pulp run"
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "pulp": "^12.0.1",
    "purescript": "^0.11.7"
  }
}
